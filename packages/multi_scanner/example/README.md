# multi_scanner_example

Demonstrates how to use the multi_scanner plugin.

## Getting Started

Для запуска проекта нужно выполнить генерацию кода в /multiscanner/example:

```sh
cd /multiscanner/example
```

```sh
dart run build_runner build --delete-conflicting-outputs
```

